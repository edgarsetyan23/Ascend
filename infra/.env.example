# Copy this to infra/.env — required for every CDK deploy
# Run: source infra/.env && npx cdk deploy --require-approval never

OWNER_USER_ID=<cognito-sub-of-the-app-owner>
# To find it: aws dynamodb scan --table-name AscendData --projection-expression "PK" \
#   --region us-east-1 --output json | grep -o '"USER#[^"]*"' | sort -u
